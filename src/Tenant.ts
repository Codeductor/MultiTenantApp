// Branding settings (could be loaded dynamically in a real multi‑tenant app)
const tenant = {
    primaryColor: '#6200ee',
    brandName: 'MultiTenantApp',
    appVersion: '0.0.1',
};

export default tenant;